/**
 * Deterministic photo URLs used by Image demos. The `seed` segment pins the
 * image so it never changes between runs, keeping demos visually stable.
 */

/** A landscape photo (640x320). */
export const SAMPLE_A = 'https://picsum.photos/seed/clay-landscape/640/320';

/** A second, distinct photo for crossfade demos (640x320). */
export const SAMPLE_B = 'https://picsum.photos/seed/clay-city/640/320';
